#include <iostream>
#include<cmath>
using namespace std;

int main()
{
    double n, P = 1;
    cin  >> n;
    for (double i = 1; i < n + 1; i++) {
        P *= 1 - (1 / (2 * i));
        cout << "P="<<P;
    }
}
